// SINGLE SOURCE OF TRUTH — change this one value to switch environments.
// Frontend and backend are served from the same origin (see README
// Architecture), so this is a path, not a full URL — no CORS involved.
const API_BASE = '/freshgo/backend/api';
// Local dev (XAMPP, project mounted at htdocs/freshgo/):
// const API_BASE = '/freshgo/backend/api';
